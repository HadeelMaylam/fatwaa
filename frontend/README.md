# Fatwa RAG Frontend 🕌

واجهة بحث الفتاوى - مبنية بـ Next.js 14 و Tailwind CSS

## 🚀 البدء السريع

### 1. تثبيت المكتبات

```bash
cd frontend
npm install
```

### 2. تشغيل السيرفر

```bash
npm run dev
```

السيرفر يشتغل على: `http://localhost:3000`

---

## 📁 الهيكل

```
frontend/
├── src/
│   ├── app/
│   │   ├── page.tsx           # صفحة البحث
│   │   ├── results/
│   │   │   └── page.tsx       # صفحة النتائج
│   │   ├── layout.tsx         # Layout رئيسي
│   │   └── globals.css        # Styles عامة
│   └── components/            # مكونات (فارغ حالياً)
├── public/                    # ملفات ثابتة
├── package.json
└── tailwind.config.js
```

---

## 🎨 التصميم

- **اللون الأساسي:** أخضر فاتح (Sage)
- **الخط:** يدعم العربية (RTL)
- **المكونات:** بطاقات مع shadows و transitions

---

## 🔗 الربط مع Backend

Frontend يتصل مع FastAPI على: `http://localhost:8000`

**تأكد أن Backend شغال:**

```bash
# في terminal منفصل
cd ..
venv\Scripts\activate
python app/main.py
```

---

## 📝 الصفحات

### 1. صفحة البحث (`/`)
- مربع بحث
- أمثلة جاهزة
- تصميم بسيط ونظيف

### 2. صفحة النتائج (`/results`)
- عرض النتائج في بطاقات
- معلومات الشيخ والسلسلة
- السؤال والجواب الكامل
- رابط المصدر

---

## 🛠️ الأوامر

```bash
# تطوير
npm run dev

# Build للإنتاج
npm run build

# تشغيل الإنتاج
npm start

# Lint
npm run lint
```

---

## 🌐 النشر

### Vercel (موصى به)

1. رفع الكود على GitHub
2. اربط Vercel بـ GitHub
3. Deploy تلقائياً!

### طريقة أخرى

```bash
npm run build
# ارفع مجلد .next و public
```

---

## ⚙️ التخصيص

### تغيير الألوان

عدل `tailwind.config.js`:

```js
colors: {
  'sage': {
    // الألوان هنا
  }
}
```

### تغيير Backend URL

عدل في `src/app/results/page.tsx`:

```ts
const response = await axios.post(
  'YOUR_BACKEND_URL/api/search',
  // ...
)
```

---

**مبني بـ ❤️ مع Next.js 14**
